package PackagesAndInterfaces.Interfaces;

// interface b includes meth1() and meth2(), 
// after all he add meth3().

public interface B extends A {

	// interface B extends interface A
	
	void meth3();
}
